#!/usr/bin/env bash
# AI GENERATED FILE: DO NOT EDIT, REGENERATE FROM dump_to_rockable.msp INSTEAD.
# Exports a .dump checkpoint file's particles to a plain XYZ file:
#   <particle count>
#   <domain bounds upper corner x> <y> <z>
#   <type> <x> <y> <z>
#   ...
#
# Usage:
#   ./ConvExaDEMToXYZ <path/to/file.dump> <path/to/file.xyz> [path/to/shapes.shp] [extra exaDEM args]
#   ./ConvExaDEMToXYZ --last <path/to/file.xyz> [--input-dir=DIR] [extra exaDEM args]
#
# --last finds the highest-iteration exadem_*.dump under <input-dir>/CheckpointFiles/ (default
# input-dir: ExaDEMOutputDir, matching restart_template.py's convention) and, if present,
# <input-dir>/CheckpointFiles/RestartShapeFile.shp as the shape file -- so you don't have to look
# them up yourself.
#
# Without a .shp file, the "type" column is the numeric type index from the .dump (a .dump alone
# has no shape-name mapping). With one, it maps each type index to its shape name (by
# registration order).
#
# --type-map=[S1,S2,S3] maps each type index to a name directly (by position), without needing a
# .shp file; it takes priority over a .shp file's names if both are given.
#
# Which of the known .dump field-set combinations (interaction/fragmentation, with/without a
# "group" field) matches this dump is auto-detected by the dump_to_xyz operator itself.
#
# The exaDEM binary is looked up on PATH by default; set EXADEM_BIN to point at a specific one.
set -euo pipefail

LAST=false
INPUT_DIR="ExaDEMOutputDir"
TYPE_MAP=""
ARGS=()
for arg in "$@"; do
  case "$arg" in
    --last)
      LAST=true
      ;;
    --input-dir=*)
      INPUT_DIR="${arg#--input-dir=}"
      ;;
    --type-map=*)
      TYPE_MAP="${arg#--type-map=}"
      ;;
    *)
      ARGS+=("$arg")
      ;;
  esac
done
set -- "${ARGS[@]+"${ARGS[@]}"}"

if [ "$LAST" = true ]; then
  if [ $# -lt 1 ]; then
    echo "Usage: $0 --last <path/to/file.xyz> [--input-dir=DIR] [extra exaDEM args]" >&2
    exit 1
  fi
  XYZ_FILE="$1"
  shift

  CHECKPOINT_DIR="$INPUT_DIR/CheckpointFiles"
  if [ ! -d "$CHECKPOINT_DIR" ]; then
    echo "Error: no checkpoint directory found at $CHECKPOINT_DIR (use --input-dir=DIR to override)" >&2
    exit 1
  fi

  LAST_ITER=-1
  for f in "$CHECKPOINT_DIR"/exadem_*.dump; do
    [ -e "$f" ] || continue
    base="$(basename "$f")"
    num="${base#exadem_}"
    num="${num%.dump}"
    num=$((10#$num))  # force base 10: these are zero-padded (e.g. 0000012345)
    [ "$num" -gt "$LAST_ITER" ] && LAST_ITER=$num
  done
  if [ "$LAST_ITER" -lt 0 ]; then
    echo "Error: no exadem_*.dump file found in $CHECKPOINT_DIR" >&2
    exit 1
  fi

  DUMP_FILE="$(printf "%s/exadem_%010d.dump" "$CHECKPOINT_DIR" "$LAST_ITER")"
  echo "Using latest checkpoint: $DUMP_FILE" >&2

  SHAPE_FILE="$CHECKPOINT_DIR/RestartShapeFile.shp"
  if [ ! -f "$SHAPE_FILE" ]; then
    echo "No RestartShapeFile.shp found in $CHECKPOINT_DIR, the 'type' column will be numeric type indices." >&2
    SHAPE_FILE=""
  fi
else
  if [ $# -lt 2 ]; then
    echo "Usage: $0 <path/to/file.dump> <path/to/file.xyz> [path/to/shapes.shp] [extra exaDEM args]" >&2
    echo "   or: $0 --last <path/to/file.xyz> [--input-dir=DIR] [extra exaDEM args]" >&2
    exit 1
  fi
  DUMP_FILE="$1"
  XYZ_FILE="$2"
  shift 2

  SHAPE_FILE=""
  if [ $# -ge 1 ] && [[ "$1" != --* ]]; then
    SHAPE_FILE="$1"
    shift
  fi
fi

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
EXADEM_BIN="${EXADEM_BIN:-exaDEM}"
DUMP_TO_XYZ_MSP="${DUMP_TO_XYZ_MSP:-$SCRIPT_DIR/dump_to_xyz.msp}"

TMP_MSP="$(mktemp --suffix=.msp)"
trap 'rm -f "$TMP_MSP"' EXIT

{
  echo "dump_to_xyz_target:"
  echo "  - dump_to_xyz:"
  echo "     filename: \"$DUMP_FILE\""
  echo "     xyz_filename: \"$XYZ_FILE\""
  if [ -n "$SHAPE_FILE" ]; then
    echo "     shape_filename: \"$SHAPE_FILE\""
  fi
  if [ -n "$TYPE_MAP" ]; then
    echo "     type_map: $TYPE_MAP"
  fi
} > "$TMP_MSP"

"$EXADEM_BIN" "$DUMP_TO_XYZ_MSP" "$TMP_MSP" "$@"
